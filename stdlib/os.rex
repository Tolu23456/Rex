/// os.rex — Operating System utilities for Rex.

/// args() — Returns command-line arguments.
prot args():
    pass

/// exit(code) — Terminate process with exit code.
prot exit():
    pass

/// getenv(name) -> str — Get environment variable.
prot getenv() -> str:
    return ""

/// time_ns() -> int — Returns monotonic time in nanoseconds.
prot time_ns() -> int:
    return 0

/// sleep(ms) — Sleep for N milliseconds.
prot sleep():
    pass
