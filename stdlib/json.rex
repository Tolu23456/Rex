/// json.rex — Hand-written JSON parser and serializer in Rex.
/// Implemented using current features (str, seq, dict, if, while, for, prot).

/// parse() — Parse JSON object.
prot parse():
    pass

/// stringify() -> str — Serialize dictionary to JSON string.
prot stringify() -> str:
    return "{}"
